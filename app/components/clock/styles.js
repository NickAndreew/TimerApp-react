import { StyleSheet } from 'react-native'

const clockStyles = StyleSheet.create({
  count: {
    fontSize: 44,
    fontWeight: 'bold',
  },
  timerView: {
    width: '100%',
    alignItems: 'center',
  }
})

export { clockStyles }